[project]
name = "geez-amharic-translator"
version = "0.1.0"
description = "English→French and Ge'ez→Amharic neural MT"
readme = "README.md"
requires-python = ">=3.10,<3.13"
# Streamlit Cloud installs from requirements.txt only (see README).
# Notebook / local dev extras:
dependencies = []

[project.optional-dependencies]
dev = [
    "matplotlib>=3.10.9",
    "pdfplumber>=0.11.8",
    "sacrebleu>=2.4.0",
    "jupyter>=1.0.0",
]
